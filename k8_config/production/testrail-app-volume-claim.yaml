apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: testrail-app-volume-claim
  namespace: testrail-import-production
spec:
  storageClassName: testrail-app
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
